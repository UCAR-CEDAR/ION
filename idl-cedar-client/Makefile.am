
# Build the new idl client.

AUTOMAKE_OPTIONS = foreign check-news
ACLOCAL_AMFLAGS = -I conf

prodir = $(idl_pro_dir)

nobase_dist_pro_DATA = aggregate.pro cedar_data.pro dods_variables.pro \
			get_kindat_list.pro opendap_parse_das.pro

EXTRA_DIST =

CLEANFILES = *~ *.sum *.log

